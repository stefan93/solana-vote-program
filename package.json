{
  "name": "helloworld",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/client/main.ts",
    "start-with-test-validator": "start-server-and-test 'solana-test-validator --reset --quiet' http://localhost:8899/health start",
    "test-validator": "start-server-and-test 'solana-test-validator --reset --quiet' http://localhost:8899/health",
    "lint": "eslint --ext .ts src/client/* && prettier --check \"src/client/**/*.ts\"",
    "lint:fix": "eslint --ext .ts src/client/* --fix && prettier --write \"src/client/**/*.ts\"",
    "clean": "npm run clean:program-c && npm run clean:program-rust",
    "build:program-c": "V=1 make -C ./src/program-c helloworld",
    "clean:program-c": "V=1 make -C ./src/program-c clean",
    "build:program-rust": "cargo build-bpf --manifest-path=./src/program/Cargo.toml --bpf-out-dir=dist/program",
    "clean:program-rust": "cargo clean --manifest-path=./src/program-rust/Cargo.toml && rm -rf ./dist",
    "test:program-rust": "cargo test-bpf --manifest-path=./src/program-rust/Cargo.toml",
    "pretty": "prettier --write '{,src/**/}*.ts'",
    "build-deploy": "cargo build-bpf --manifest-path=./src/program/Cargo.toml --bpf-out-dir=dist/program && solana program deploy /Users/stefanpantic/private/projects/rust/token/dist/program/token_program.so",
    "integration-test": "npm run test-validator build-deploy start",
    "test": "npm run test:e2e",
    "test:e2e": "start-server-and-test 'solana-test-validator -r -q' http://localhost:8899/health 'npm run build-deploy && jest test/e2e ' ",
    "foo": ""
  },
  "dependencies": {
    "@solana/web3.js": "^1.33.0",
    "@solana/buffer-layout-utils": "0.2.0",
    "borsh": "^0.7.0",
    "mz": "^2.7.0",
    "yaml": "^2.0.0"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/eslint": "^8.2.2",
    "@types/eslint-plugin-prettier": "^3.1.0",
    "@types/mz": "^2.7.2",
    "@types/prettier": "^2.1.5",
    "@types/yaml": "^1.9.7",
    "@typescript-eslint/eslint-plugin": "^4.6.0",
    "@typescript-eslint/parser": "^4.6.0",
    "@types/jest": "^28.1.7",
    "eslint": "^7.12.1",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.1.2",
    "start-server-and-test": "^1.11.6",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.5",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
